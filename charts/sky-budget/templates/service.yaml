apiVersion: v1
kind: Service
metadata:
  name: {{ include "sky-budget.fullname" . }}
  labels:
    {{- include "sky-budget.labels" . | nindent 4 }}
  annotations:
    cloud.google.com/backend-config: '{"default": "{{ include "sky-budget.fullname" . }}"}'
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sky-budget.selectorLabels" . | nindent 4 }}
